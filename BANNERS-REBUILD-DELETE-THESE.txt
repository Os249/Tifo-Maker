Banners rebuild — five files to delete in C:\Users\PCD\Desktop\tifo-maker

Nothing imports them. I could not delete them from here, so I emptied them
instead: each is now a comment and `export {}`. That means `npm run build`
passes while they are there (before, it failed on them, and so would the
Docker build Railway runs). They should still go.

PowerShell, from the repo root:

    Remove-Item scripts\banner-physics.mts
    Remove-Item src\core\bannerMigrate.ts
    Remove-Item src\render\simulator\bannerFit.ts
    Remove-Item src\render\simulator\cloth.ts
    Remove-Item src\render\simulator\standHeightfield.ts

Optional — the same kind of leftover, still compiling, nothing uses it:

    Remove-Item src\ui\bannerStudio.ts

GitHub Desktop will list them as deleted; commit that with the rest.

What they were:

    cloth.ts              the XPBD cloth solver
    standHeightfield.ts   the baked collider it checked against
    bannerFit.ts          sizing against a free position
    bannerMigrate.ts      the retired Banner Studio's importer
    banner-physics.mts    the old solver's test harness
    bannerStudio.ts       the retired Banner Studio itself
